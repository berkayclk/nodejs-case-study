import * as RecordMiddleware from './record.middleware';
import * as RecordController from './record.controller';

export const RecordMiddlewares = RecordMiddleware;
export const RecordControllers = RecordController;
